var app = app || {};

(function () {
// ----- User Model
	app.User = Parse.User.extend('User', {

		//prepopulate player with default data, if none exists
		initialize: function(){
			
		},

		

	});

})();